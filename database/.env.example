# Database package — copy to .env
# Local: matches docker-compose.yml at repo root
# Production (Neon): use pooled + direct URLs from Neon dashboard

DATABASE_URL="postgresql://bmv:bmv@localhost:5434/browsemyvacations?schema=public"
DIRECT_URL="postgresql://bmv:bmv@localhost:5434/browsemyvacations?schema=public"

# Neon example (replace with your values):
# DATABASE_URL="postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/browsemyvacations?sslmode=require"
# DIRECT_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/browsemyvacations?sslmode=require"

NODE_ENV=development
