# Allow package lifecycle/build scripts (needed on Hostinger for sharp, esbuild, prisma, etc.)
ignore-scripts=false
enable-pre-post-scripts=true

# pnpm 10+/11 blocks native builds until approved — allow them in CI/hosting
dangerouslyAllowAllBuilds=true
