# Keep lifecycle scripts enabled during install
ignore-scripts=false
enable-pre-post-scripts=true

# pnpm 11 also reads this from pnpm-workspace.yaml; keep both for Hostinger/corepack
dangerouslyAllowAllBuilds=true
